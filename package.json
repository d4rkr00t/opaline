{
  "name": "@opaline/monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "scripts": {
    "test": "ava",
    "build": "tsc -b ./",
    "lint:staged": "lint-staged",
    "postinstall": "npm run build"
  },
  "keywords": [],
  "license": "MIT",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "dependencies": {
    "ava": "^2.2.0",
    "lint-staged": "^9.2.1",
    "pre-commit": "^1.2.2",
    "prettier": "^1.18.2",
    "ts-node": "^8.3.0",
    "typescript": "^3.5.3"
  },
  "lint-staged": {
    "*.{js,ts}": [
      "prettier --write",
      "git add"
    ]
  },
  "pre-commit": [
    "lint:staged"
  ],
  "ava": {
    "compileEnhancements": false,
    "extensions": [
      "ts"
    ],
    "require": [
      "ts-node/register"
    ]
  }
}
