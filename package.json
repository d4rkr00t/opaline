{
  "name": "@opaline/monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "scripts": {
    "test": "ava",
    "build": "tsc -b ./",
    "lint:staged": "lint-staged",
    "postinstall": "npm run build"
  },
  "keywords": [],
  "license": "MIT",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "dependencies": {
    "ava": "^2.2.0",
    "lint-staged": "^10.0.8",
    "pre-commit": "^1.2.2",
    "prettier": "^1.19.1",
    "ts-node": "^8.6.2",
    "typescript": "^3.8.2"
  },
  "lint-staged": {
    "*.{js,ts}": [
      "prettier --write",
      "git add"
    ]
  },
  "pre-commit": [
    "lint:staged"
  ],
  "ava": {
    "compileEnhancements": false,
    "extensions": [
      "ts"
    ],
    "require": [
      "ts-node/register"
    ]
  }
}
