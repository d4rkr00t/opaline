{
  "name": "nextcli-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "scripts": {
    "test": "ava",
    "build": "tsc -b ./",
    "lint:staged": "lint-staged",
    "postinstall": "npm run build"
  },
  "keywords": [],
  "license": "MIT",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "dependencies": {
    "ava": "^1.3.1",
    "lint-staged": "^8.1.5",
    "pre-commit": "^1.2.2",
    "prettier": "^1.16.4",
    "typescript": "^3.4.1"
  },
  "lint-staged": {
    "*.{js,ts}": [
      "prettier --no-editorconfig --write",
      "git add"
    ]
  },
  "pre-commit": [
    "lint:staged"
  ],
  "ava": {
    "compileEnhancements": false,
    "extensions": [
      "ts"
    ],
    "require": [
      "ts-node/register"
    ]
  }
}
